---
- name: Change password
  hosts: all
  become: true
  tasks:
    - name: "Update {{ ansible_user }} password"
      user:
        name: "{{ ansible_user }}"
        password: "{{ password }}"
        update_password: always
