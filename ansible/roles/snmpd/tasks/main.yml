---
- name: Open required ports
  ufw:
    rule: allow
    port: "161"
    proto: udp

- name: Install snmpd
  apt:
    name: snmpd
    state: present

- name: Configure snmpd
  template:
    src: snmpd.conf.j2
    dest: /etc/snmp/snmpd.conf
    mode: '0600'
  notify: restart snmpd