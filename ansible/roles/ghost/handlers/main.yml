---
- name: restart ghost
  command:
    cmd: ghost start
    chdir: /var/www/ghost
  become: yes
  become_user: idiotghost

- name: enable ghost
  command:
    cmd: ghost enable
    chdir: /var/www/ghost
  become: yes
  become_user: idiotghost