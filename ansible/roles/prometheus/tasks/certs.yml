---
- name: Create certs folder
  file:
    state: directory
    name: "/etc/prometheus/certs/{{ item }}"

- name: "Add {{ item }}.crt"
  template:
    src: "../../../../certs/{{ item }}/client.crt.j2"
    dest: "/etc/prometheus/certs/{{ item }}/client.crt"

- name: "Add {{ item }}.pem"
  template:
    src: "../../../../certs/{{ item }}/client.pem.j2"
    dest: "/etc/prometheus/certs/{{ item }}/client.pem"